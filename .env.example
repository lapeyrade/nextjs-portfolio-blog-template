# Example environment variables (do NOT commit real secrets)
# Copy this to a local `.env` or use your platform's secret store.

# Server-only secrets (must NOT be prefixed with NEXT_PUBLIC_)
RESEND_API_KEY=your-resend-api-key-here
WEBVITALS_API_KEY=your-webvitals-api-key-here
CONTACT_EMAIL=you@example.com
FROM_EMAIL=no-reply@example.com

# Public/Client-safe values (allowed to be prefixed with NEXT_PUBLIC_)
NEXT_PUBLIC_SITE_URL=https://example.com
NEXT_PUBLIC_WEBVITALS_SAMPLE=0.05
NEXT_PUBLIC_WEBVITALS_BATCH_MAX=10
